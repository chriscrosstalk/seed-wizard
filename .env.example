# SQLite Database Configuration
# Path to the SQLite database file (optional - defaults to ./data/seed-wizard.db)
# DATABASE_PATH=/app/data/seed-wizard.db

# Anthropic API Key (for AI seed extraction)
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================================
# MIGRATION ONLY (can be removed after migrating from Supabase)
# ============================================================
# These are only needed when running the migration script:
#   npx tsx scripts/migrate-from-supabase.ts
#
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_SERVICE_KEY=your-service-role-key-here
